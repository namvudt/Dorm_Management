import React from 'react';
import { Box, Typography } from '@mui/material';

const NotFound = () => (
  <Box textAlign="center" mt={5}>
    <Typography variant="h4" color="error">404 - Không tìm thấy trang</Typography>
  </Box>
);

export default NotFound;
